version: '3.8'
services:
  sysml-parser:
    build: .
    ports:
      - "0.0.0.0:9080:9080"
    environment:
      - SERVER_PORT=9080
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9080/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
