server:
  port: 8080

spring:
  application:
    name: sysml-ast-parser
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

sysml:
  parser:
    version: "1.0"
    max-input-size: 1000000
